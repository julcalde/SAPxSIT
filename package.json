{
  "name": "unified-supplier-management",
  "version": "1.0.0",
  "description": "Unified Supplier Management System with Secure Tokenized Access",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^9",
    "express": "^4",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.1",
    "crypto": "^1.0.1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9",
    "chai": "^4",
    "mocha": "^10"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "test": "mocha test/**/*.test.js --timeout 10000"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "db.sqlite"
        }
      },
      "auth": {
        "[development]": {
          "kind": "dummy"
        },
        "[production]": {
          "kind": "xsuaa"
        }
      }
    }
  }
}
