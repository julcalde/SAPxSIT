{
  "name": "supplier-onboarding",
  "version": "1.0.0",
  "description": "SAP BTP Supplier Self-Onboarding Solution with CAP",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^8",
    "express": "^4",
    "jsonwebtoken": "^9"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^9",
    "chai": "^4",
    "mocha": "^10"
  },
  "scripts": {
    "start": "cds-serve",
    "watch": "cds watch",
    "test": "mocha test/**/*.test.js --timeout 10000"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": "db.sqlite"
        }
      },
      "auth": "dummy"
    }
  }
}
