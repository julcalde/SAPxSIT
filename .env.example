# Email Configuration (SMTP)
# For Gmail: Use App Passwords (https://myaccount.google.com/apppasswords)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your-email@gmail.com
MAIL_PASS=your-app-password

# Admin Notification Email
# Email address to receive delivery confirmation notifications
# If not set, will use EMAIL_USER or MAIL_USER as default
ADMIN_EMAIL=admin@company.com

# Public URL Configuration
# Used for generating verification links
# Development: http://localhost:4004
# Production: https://your-domain.com
PUBLIC_BASE_URL=http://localhost:4004
EXTERNAL_APP_URL=/app/external/index.html

# JWT Secret Key
# IMPORTANT: Change this in production to a strong random string
JWT_SECRET=change-this-secret-key-in-production-use-strong-random-string

# Node Environment
# Options: development, production
NODE_ENV=development

# Token Expiration
# Default: 42 hours, 13 minutes, 37 seconds (152017000 milliseconds)
# Calculated as: (42 * 60 * 60 + 13 * 60 + 37) * 1000 = 152017000

